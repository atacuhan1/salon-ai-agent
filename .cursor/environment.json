{
  "name": "salon-ai-agent",
  "install": "npm ci && bash scripts/ensure-postgres.sh && npx prisma generate && DATABASE_URL=postgresql://salon:salon@127.0.0.1:5432/salon npx prisma migrate deploy",
  "start": "bash scripts/ensure-postgres.sh",
  "terminals": [
    {
      "name": "Next.js dev server",
      "command": "npm run dev -- --hostname 0.0.0.0 --port 3000",
      "description": "Next.js App Router development server on port 3000."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
